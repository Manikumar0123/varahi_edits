# ============================================================
# VARAHI EDITS - Application Configuration
# ============================================================

# Server
server.port=${PORT:8080}

# ============================================================
# Database (Railway PostgreSQL)
# ============================================================

spring.datasource.url=jdbc:postgresql://gondola.proxy.rlwy.net:53048/railway
spring.datasource.username=postgres
spring.datasource.password=${PGPASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
#SPRING_DATASOURCE_URL=jdbc:postgresql://db.hbevchprvurycaisznrb.supabase.co:5432/postgres?sslmode=require
#SPRING_DATASOURCE_USERNAME=postgres
#SPRING_DATASOURCE_PASSWORD=Manikumar$pithani@0123


#SPRING_DATASOURCE_DRIVER_CLASS_NAME=org.paostgresql.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.defer-datasource-initialization=true

# ============================================================
# Email (Gmail SMTP)
# ============================================================

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=varahiedits@gmail.com
spring.mail.password=${EMAIL_PASSWORD:test123}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

spring.profiles.active=default

# Business Email
app.email.business=varahiedits@gmail.com
app.email.from=varahiedits@gmail.com

# ============================================================
# JWT
# ============================================================

#app.jwt.secret=${JWT_SECRET}
app.jwt.secret=${JWT_SECRET:defaultSecret123}
app.jwt.expiration=86400000

# ============================================================
# Admin
# ============================================================

app.admin.username=admin
app.admin.password=${ADMIN_PASSWORD}

# ============================================================
# Twilio
# ============================================================

twilio.account.sid=${TWILIO_SID}
twilio.auth.token=${TWILIO_TOKEN}
twilio.whatsapp.from=${TWILIO_WHATSAPP_FROM}
twilio.sms.from=${TWILIO_SMS_FROM}
app.whatsapp.business=${APP_WHATSAPP_BUSINESS}

# ============================================================
# CORS
# ============================================================

app.cors.allowed-origins=http://localhost:3000,http://localhost:5500,http://127.0.0.1:5500

# Logging
logging.level.com.varahiedits=INFO
spring.mvc.converters.preferred-json-mapper=jackson

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB